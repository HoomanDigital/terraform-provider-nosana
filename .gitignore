# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Go workspace file
go.work

# Terraform files
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
.terraform/
.terraform.lock.hcl
terraform.log

# Crash log files
crash.log

# Local provider development overrides
terraform.rc
.terraformrc

# Provider plugin
terraform-provider-*

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Nosana keypairs and sensitive files
nosana_key.json
.nosana/
wallet.json

# GPG Keys and signing (NEVER commit these!)
*.key
*.asc
gpg_*
*_SHA256SUMS*

# Private registry files (NEVER commit these!)
.provider_id
.key_id
.shasums_*
version.json
key.json
provider.json

# Environment and secrets
.env*
*.pem
*.key
secrets/
private/

# API tokens and credentials
*token*
*secret*
*credential*

# Terraform provider binaries (exclude from repo)
terraform-provider-nosana*

# Local development files
.env
.env.local
local.tf
test-local.tf

# Temporary files
tmp/
temp/

# Build artifacts
dist/
build/
bin/

# Log files
*.log

# node_modules
node_modules/